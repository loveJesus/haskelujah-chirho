<!-- For God so loved the world, that he gave his only begotten Son, that whosoever believeth in him should not perish, but have everlasting life. — John 3:16 (KJV) -->

# Boot-class corpus diagnostic Chirho

Run the shared `family-equations-chirho/closed-injectivity-chirho/corpus-chirho/run_chirho.ts`
from the worktree root with the explicitly rebuilt CLI and this output directory.
This is one diagnostic pass per axis, not a replacement for the published gates.
The existing 938/767 denominators and error-code-or-panic verdict detector stay
unchanged. Actual panic headers are recorded separately from echoed source text.

The instrument admits four concurrent children with a fifteen-second deadline;
initial timeouts are retained and rerun alone with sixty seconds. An abnormal
exit invalidates the run even when a diagnostic preceded it. CLI hashes, tracked
diffs and changed/new compiler source paths and hashes must remain stable.
Deleted source paths are recorded separately so a structural move is not an
unreadable-file exception in the measuring instrument.

Compare sets, not only totals, with the preceding closed-injectivity checkpoint
and with the committed main lists. Audit every changed file's reason before
claiming capability. Neither this diagnostic nor a branch checkpoint authorizes
merging main with outstanding main-relative accept regressions.

The root observations are the first frozen diagnostic:890/938 and264/767.
They exposed the annotated-variable default regression T18585. `final-chirho/`
retains the post-repair diagnostic:891/938 and264/767, with only T18585 moving
relative to the first run. Both runs have no unresolved timeouts, abnormal exits
or actual panic headers. CLI and compiler source hashes remain stable in each.

`comparison-chirho.json` records exact set differences. Relative26f77cad:
eight accept recoveries, no accept losses; eleven reject gains and six losses.
The recoveries are T16204a/b, T17772, T18129, T20588d_aux, T20661_aux, T21205
and T21558. T16204a/b, T17772 and T21205 now retain their written associated
result kinds; T21558 no longer treats leading `*` as malformed Nat multiplication.
T18129's prior type-application kind error disappears, but it has no associated
family declaration: no narrower causal attribution is claimed without isolation.
The two auxiliary files recover through actual concrete boot-class agreement,
not exclusion from the corpus. Main-relative18 gains/9 losses still block merge.

`reject-audit-claude-chirho.md` displays Claude's independent audit of all17
reject deltas from the first diagnostic, checked against source and committed
GHC stderr. Only trailing whitespace is normalized; `reject-audit-raw-chirho.json`
preserves the unchanged original text. Nine gains match the reference rule and two originally had adjacent
arity wording. The final run makes AssocTyDef05/06 explicit arity errors (one
required parameter against zero/two); all eleven gains now name the applicable
rule. Some diagnostic source anchors still differ from GHC. All six lost old
rejections had wrong reasons; the actual missing GHC rules remain named in the
audit. Neither their disappearance nor the raw264 count is a new capability
claim about every rejected corpus file.
