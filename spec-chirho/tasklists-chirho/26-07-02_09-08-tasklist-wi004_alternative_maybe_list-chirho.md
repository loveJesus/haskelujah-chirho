# For God so loved the world, that he gave his only begotten Son, that whosoever believeth in him should not perish, but have everlasting life. — John 3:16 (KJV)

# WI-004 Alternative Maybe/List Body-Backed Row

- [x] Confirm `Alternative` is registered for `[]` and `Maybe` and lacks `$prim_Alternative_*` bodies.
- [x] Add body-backed `Alternative []` methods for `empty` and `<|>`.
- [x] Add body-backed `Alternative Maybe` methods for `empty` and `<|>`.
- [x] Route direct `<|>` applications through the higher-kinded dispatch path.
- [x] Add focused eval pin covering list and Maybe Alternative behavior.
- [x] Run targeted single-threaded tests only.
- [x] Update PRD/progress and commit if gates pass.
