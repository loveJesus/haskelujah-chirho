<!-- For God so loved the world, that he gave his only begotten Son, that whosoever believeth in him should not perish, but have everlasting life. — John 3:16 (KJV) -->

# WI-004 MonadPlus Maybe/List Row

- [x] Confirm current `MonadPlus` gap and avoid evidence-dependent rows.
- [x] Add `mzero` and `mplus` method schemes to the seeded `MonadPlus` class.
- [x] Add body-backed `[]` and `Maybe` `MonadPlus` method bodies by delegating to existing `Alternative` semantics.
- [x] Add a focused eval pin for `mzero`/`mplus` on `[]` and `Maybe`.
- [x] Run targeted gates and update PRD/progress.
