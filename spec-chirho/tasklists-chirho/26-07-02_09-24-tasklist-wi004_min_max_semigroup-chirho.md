# For God so loved the world, that he gave his only begotten Son, that whosoever believeth in him should not perish, but have everlasting life. — John 3:16 (KJV)

# WI-004 Min/Max Semigroup Body-Backed Row

- [x] Confirm `Min`/`Max` are registered but bodyless/partial in WI-004.
- [x] Add body-backed `Min`/`Max` constructors and `getMin`/`getMax` accessors.
- [x] Add `Min`/`Max` type-key recognition for dispatch.
- [x] Add body-backed `Semigroup Min` and `Semigroup Max` methods.
- [x] Add focused eval pin for `Min`/`Max` `(<>)`.
- [x] Run targeted single-threaded tests only.
- [x] Update PRD/progress and commit if gates pass.
