# GHC should_compile — timed measurement artifact (John 3:16)
# measured: 2026-09-24 UTC, AFTER occurrence provenance for references AND the literal
#   carrier. Still a PARTIAL migration: see TRANSPORT LIMITS below for exactly how far
#   the literal channel reaches.
# code measured: e8cd6bf4d68b46448d21667e2cf4c15000e67e1d (HASKELUJAH/claude_chirho,
#   branch provenance-literals-chirho, pushed as gh_chirho/provenance-literals-3-chirho;
#   a new remote name because the older ref holds the pre-rebase line and no force push
#   was taken)
# build: explicit `cargo build -p haskelujah`, DEBUG profile (as every earlier artifact),
#   macOS arm64; measured binary: frozen copy at
#   tmp-chirho/literal-corpus-chirho/haskelujah-literal-chirho
#   LOCATOR: that path is relative to the REPOSITORY ROOT of the worktree that measured
#   it. Earlier artifacts wrote `../haskelujah-claude-chirho/tmp-chirho/...`, which
#   resolves only from a sibling lane directory and not from a repository root at all.
# CLI SHA256: fd0da8094f4086db49148e4ddfa31658e08a371bfa16acbdc0ca41f53a28db4a
#   digest asserted unchanged before and after all four corpus passes, and again before
#   and after the execution set
# method: haskelujah check <file>, timeout 15s/file, parallelism 4, timeouts re-run
#   alone at 60s (none occurred). The verdict rule is the COMMITTED one, sourced from
#   workflows-chirho/testing-chirho/corpus-runner-chirho/classify_chirho.sh, applied in
#   this order: TIMEOUT (exit 124), then ABNORMAL (any exit but 0 or 1), then PANIC (a
#   real Rust panic header), then REJECT (`error[E` AND exit 1), then ACCEPT (exit 0 and
#   no `error[E`). A panic header is the shape the runtime prints, `thread '<name>'
#   panicked at ...` with an optional ALL-DIGIT thread id, and it must begin a line.
#   The BARE SUBSTRING `panic` is never a verdict, because source can contain it: the
#   earlier wording in this artifact ("a rejection contains `error[E` or `panic`")
#   described a rule that no longer exists and is corrected here. Matching is pure
#   shell, never interactive `grep`, which is shadowed in this environment by a ugrep
#   wrapper with content-dependent `-q` behaviour. The runner records each file's EXIT
#   CODE beside its verdict, and no accepted file exited nonzero.
# corpus: ghc-tests-chirho/typecheck-chirho/should_compile — 938 .hs files
#   (find-based; T16234/ included); per-file source SHA256s retained with the receipts
#
# RESULT: PASS=885 FAIL=53 TOTAL=938
# STABILITY: DETERMINISTIC — two complete passes, zero timeouts, zero nonzero-exit
#   accepts; the two passes are byte-identical to each other
# QUOTE-AS: ~94% (885 of 938) — do not quote a third significant figure
#
# QUOTE BOTH AXES TOGETHER. The companion artifact is the fraction 235 of 767 (30.6%), published without a single label.
# THE CARRY RULE IS EXHAUSTED ON THE REJECT AXIS. Earlier artifacts carried the
# existing approximate label forward with current counts, which stayed honest while
# ~29% remained a valid reading of the count. 235 of 767 is 30.6%, which is ~30%
# truncated and ~31% nearest, so no carry of ~29% is a rounding of it under either
# policy. This artifact therefore publishes the FRACTION with both readings and no
# single label: that decides nothing and states nothing false. L.J.'s uniform
# truncation-versus-nearest decision now governs one axis and one choice, 30 or 31.
# The accept label is ~94% under either policy and is unaffected.
# No policy choice or public-site deployment.
#
# CHANGE SINCE LAST MEASUREMENT (7cfd9b8a): 885 -> 885, ZERO movement.
#   The failure list is identical file for file, its SHA256 below is unchanged, and each
#   of the two passes reproduces that list independently.
#   What changed is again evidence TRANSPORT, which this typecheck axis cannot see: a
#   LITERAL now carries an origin minted where it is built, in expressions AND in
#   patterns, the desugarer copies that origin and the use's role onto the occurrence it
#   mints, and a literal with an origin is published under that origin ONLY — so no
#   legacy consumer reaches an owned literal proof by span.
# TRANSPORT LIMITS OF THE LITERAL CHANNEL, explicit (gpt_chirho #24761 corrected all
#   three of these against the code, and they are stated here rather than in a message):
#   - A float literal never becomes `fromRational`. The desugarer converts only an
#     integer literal (`fromInteger`) and, under OverloadedStrings, a string literal
#     (`fromString`); a float becomes a raw Core literal. The FractionalLiteral role is
#     therefore CAPTURE AND CARRIER ONLY, not an end-to-end path.
#   - Literal evidence is ONE {class, type-key} per origin. It is not role-keyed and not
#     multi-predicate, so it cannot represent a pattern literal's distinct Num and Eq
#     operations.
#   - A pattern literal carries an origin but the checker captures nothing for it, so it
#     publishes no evidence at all. That is a PRODUCER gap, not a transport gap.
#   The two placeholder controls test the two ENDS separately — one at capture in the
#   checker, one at the join in the driver — and are not one integrated
#   producer-to-consumer execution test.
# SOURCE-SPECIFIC GATES, each stated against the source it actually ran on.
#   The four corpus passes and the execution set both ran on e8cd6bf4's frozen CLI
#   fd0da809. The unit and integration suites ran on 8730d067, which differs from
#   e8cd6bf4 in NO crate line at all (verified mechanically: the only files that differ
#   are two spec-chirho documents). Those suites: ast 13, core 129, naming 136, parser
#   349, th 15, typing 349, driver library 1797 passed / 0 failed (592.73s); and nine
#   integration targets — canaries 7, curated 3, dictionary evidence 10, instance
#   obligations 13, method occurrences 7, native laziness 5, numeric hierarchy 5, show
#   precedence 2, typing integration 33. Zero warnings. 2873 tests.
#   EXECUTION: 28 of 35 predicates match GHC 9.14.1 byte for byte on fd0da809, and the
#   same 28 of 35 on main's own CLI e246670e, identical predicate by predicate once heap
#   pointers and LLVM register numbers are normalised. So the SEVEN failures are
#   pre-existing and none is caused by this change: derived `Ord`'s `<` in the
#   interpreter and again natively; a toEnum-built Char in the interpreter's derived
#   Show; native `mdo`; native `map (subtract 1)`; and derived `Show` dropping
#   precedence parentheses, on both engines, which this window newly LOCATED rather than
#   introduced — deriving generates a `show` built by string concatenation and no
#   `showsPrec` at all, which is why `showsPrec` on a derived type is a missing STG
#   binding and why the dictionary layer refuses a show-only row at precedence 11.
#   An earlier execution set on the same branch's CLI 6e34983c is retained as an earlier
#   receipt. The two CLIs differ in no non-test crate line: the only crate files that
#   differ between a091b2d0 and e8cd6bf4 are two compiled solely under `cfg(test)`.
#   Their digests differ because this build is not bit-reproducible; no byte-identity
#   between the two binaries is claimed anywhere.
# MEASUREMENT OF 7cfd9b8a (2026-09-22), CHANGE SINCE 11b24799: 885 -> 885, ZERO movement.
#   The failure list is identical file for file, and its SHA256 below is unchanged.
#   What changed is evidence TRANSPORT, which this typecheck axis cannot see: each
#   reference now carries an origin minted by its producer, the checker's records and
#   the desugarer's occurrences both carry it, and the join matches by origin before
#   span or position; an origin-owned proof never reaches another occurrence. Corpus
#   neutrality was measured, not inferred from check-only mode (gpt_chirho #24547).
#   EXECUTION EVIDENCE on 7cfd9b8a itself: driver library 1793 passed / 0 failed;
#   nine integration suites green; on the frozen CLI, 26 of 31 execution predicates
#   match GHC 9.14.1 byte for byte, and main's own CLI at f8eb26bb gives the same 26 of
#   31 predicate by predicate: the five failures are pre-existing (derived Ord `<` in
#   the interpreter and natively, a toEnum-built Char in the interpreter's derived Show,
#   native mdo, native `map (subtract 1)`), none caused by this change.
#
# MEASUREMENT OF 11b24799 (2026-09-20), CHANGE SINCE 8d86bc95: 885 -> 885, ZERO movement.
#   The failure list is identical file for file, and its SHA256 below is unchanged
#   from the previous artifact. Two repairs landed in this window and neither costs
#   an accepted file: an instance whose own context cannot hold no longer satisfies
#   its head (a rejection rule, so this axis is where a false positive would show),
#   and a class method's evidence is joined to its source reference by SPAN rather
#   than by position. The second repairs silent WRONG ANSWERS this axis cannot see:
#   `instance Show W where show (W n) = "W" ++ show n` printed `W3` or `WChirho 3`
#   depending only on which declaration came first.
# REPLAY: all 53 failures here, with the other axis's 235 rejections, were re-run one at
#   a time on the same frozen CLI fd0da809, keeping stdout, stderr, exit code and source
#   SHA256 per file. All 288 are exit 1 carrying `error[E`, and zero carry a panic
#   header, timeout or abnormal exit; none rests on the bare `panic` substring. Every
#   rejection is a diagnostic, not a crash.
# FAILURE-LIST SHA256: 879cacad3900068f525cfa90c548a46eb81c1ce17dc90755b6ee285c7c9ff30e
#   DEFINITION: the SHA256 of the bare filenames listed at the end of this file, one per
#   line, EACH NEWLINE-TERMINATED, sorted C-COLLATED (LC_ALL=C — every uppercase initial
#   before every lowercase one, so `TcSpecPragmas.hs` precedes `tc134.hs`). The
#   collation is part of the definition, not an incidental detail: a case-folding locale
#   sorts this IDENTICAL list to d90fc86ce2b2c0b839837f82717c19142517810b376b87eea6c167a7ba80519d
#   instead, so a measurer who did not know the collation would read an unchanged result
#   as movement. Two agents hit exactly that before establishing set equality, and the
#   runner has since been repaired to sort C-collated (see corpus-runner-chirho/
#   sort_chirho.sh, with nine controls). The verdict files retained for THIS measurement
#   were produced before that repair and are in the caller's locale order; their list
#   hashes reproduce because the hashes were computed explicitly C-collated.
#   These are typecheck verdicts, not execution; neither upstream corpus fully passes.
# EXECUTION EVIDENCE, each gate stated against the source it actually ran on. The four
#   corpus passes above ran on 11b24799. Every execution gate ran on 6ed46217, which
#   differs from 11b24799 only in the comments of one file (verified mechanically:
#   every changed crate line is a comment): the curated GHC suite passes (486.68s); the
#   driver library is 1778 passed / 0 failed / 0 ignored / 0 filtered, including five
#   direct controls on the evidence join's boundaries, two of them mutation-checked;
#   and the driver integration suites are 202 passed / 0 failed across 19 targets.
#   Logs are kept in the lane worktree, not OS tmp. An earlier curated/integration
#   pair on 73560e17 and a driver-library run on the pre-format tree of 11b24799 were
#   superseded by these rather than relabelled. This does not establish an instance
#   WITH a context at run time, which is still wrong (`instance Show a => Show (T a)`),
#   nor generic record Eq/Show dispatch. See
#   workflows-chirho/testing-chirho/execution-measurement-chirho.md.
#
# PREVIOUS MEASUREMENT (8d86bc95, 2026-09-19): 882 -> 885, +3 gained, zero lost.
#   T15079, tc124: a leading `forall` now scopes over the `=>` and `->` in its body
#     (flat type grammar; the record-field lowering bug open since 2026-08-02), so a
#     rank-n record field is lowered as written, and record construction and update
#     check the field value against that rank-n type (row 484's e028c1a3, carried).
#   T18831: `(Type :: Type)`: a depth-0 `::` inside a group is a kind annotation, not
#     an application of the type to its kind.
#   Held, each lost by a first trial and repaired for GHC's reason: T18802 (a rank-2
#     field update needs the carried record checking); T23514c (`data P5 :: forall a
#     . k -> Type`: a header's inline result kind is not under forall-or-nothing;
#     GHC 9.14.1 probes); T10808 (type-changing update where G T1 = G T2 = Int: `G t8 ~
#     G t7` on two unsolved variables is deferred, never decomposed; row 484's 327aaef7).
#   Class, instance and standalone-deriving contexts are lowered with the signature
#     grammar (tuple members, applied and multi-argument predicates, equalities, explicit
#     instance foralls). No file moves for that alone; its proof is exact GHC 9.14.1
#     outputs in tests/declaration_contexts_chirho.rs.
#
# PREVIOUS MEASUREMENT (3db3b6a6, 2026-09-10): 880 -> 882, +2 gained, zero lost.
#   T11811 and T20873: written declaration result kinds retain their head binders;
#   an inline result tail is no longer treated as the complete kind. Fresh GHC
#   9.14.1 accepts both unchanged inputs. The first trial lost three valid files;
#   preserving invisible head binders and visible forall-kind arguments recovered
#   T22560a/T22762/T23514c without weakening declaration reconciliation.
#   Five independent GHC-83865 controls now reject mismatched complete/head/tail
#   contracts. These are behavioral controls, not five upstream corpus gains.
#   Companion movement is +T22560_fail_b, -tcfail225/-ExplicitSpecificity8; its
#   header distinguishes a generic-arity rejection from the missing GHC rules.
#
# PREVIOUS MEASUREMENT (cb939e35): accept 879 -> 880 (+T14761c, no loss),
#   reject 223 -> 222 (-T14761a, no gain). Flat [] constructor application and nested
#   list delimiters repaired; T14761a's genuine GHC-10107 UNPACK warning remains open.
#
# PREVIOUS MEASUREMENT (3683dae8): 879 -> 879, zero gained, zero lost.
#   Kind inference scopes forall/quantified-constraint binders lexically and restores
#   touched environment/cache entries without discarding newly discovered free names.
#   Four fresh GHC-confirmed kind-scope execution programs improved outside this subset.
#
# PREVIOUS MEASUREMENT (18b7d1c3): 877 -> 879, +2 gained, zero lost.
#   T23764: lowercase infix type variable retained, and visible type arguments consume
#     signature binders in lexical first-occurrence order rather than allocator-ID order.
#   tc156: lowercase infix parameter retained; type-chain grouping uses the existing
#     fixity table. Parenthesizing the signature independently isolated that second defect.
#   No corpus input changed. tc192 shares the infix shape but still reaches unsupported
#     proc/x: the already-mapped arrows_proc_notation_chirho feature, not an infix gain.
#   That checkpoint did not claim full kind-dependency ordering or forall scope repair.
#
# PREVIOUS MEASUREMENT (ffa2561e, 2026-09-07): 877 -> 877. The record-field brick
#   changed construction/update: a given undeclared field (E0206), an omitted strict
#   field (E0207, GHC-95909), an omitted lazy field allowed (missing-field thunk), `C {}`
#   on any constructor. The only `C {}` in this corpus is a pattern (T26346, already
#   failing on promoted kinds, unchanged); the only construction on a constructor its file
#   does not declare (T7169, sibling module) still passes.
#
# REPRESENTATION LIMITS THAT REMAIN EXPLICIT:
#   data/newtype instances and `type data` still need real declaration shapes; GADT record
#   fields have survived since b2027c35, but refined GADT result types still need the agreed
#   constructor-argument/result AST redesign. A green `check` result around an unrepresented
#   form is not behavioral evidence; exact-output driver gates remain required.
#
# MEASUREMENT TOOLING: the runner is committed at
# workflows-chirho/testing-chirho/corpus-runner-chirho/ and every script there sources
# ONE verdict rule (classify_chirho.sh), so no measurement carries a second copy of the
# pattern. Matching is pure shell, never interactive `grep`, which is shadowed in this
# environment by a ugrep wrapper with content-dependent `-q` behaviour. The rule's order
# and the panic-header shape are stated in the method block at the top of this file; the
# bare substring `panic` is NOT a verdict. Twenty controls cover the rule, including a
# header quoted inside a diagnostic, an all-digit versus a mixed thread id, and a header
# glued to output that ended without a newline; nine more cover the ordering. Every
# repair to those rules was mutation-checked: reverting it fails the control that was
# added for it.
#
# ---- the 53 files we FAIL to accept (GHC accepts these); all deterministic ----
FloatFDs.hs
HardRecordUpdate.hs
Improvement.hs
PolytypeDecomp.hs
RebindHR.hs
RuleEqs.hs
SplitWD.hs
T10348.hs
T10856.hs
T11056.hs
T12104.hs
T12427a.hs
T13822.hs
T13848.hs
T14451.hs
T14590.hs
T15370.hs
T16188.hs
T17594a.hs
T17772.hs
T18036b.hs
T18252.hs
T20443a.hs
T20922.hs
T21010.hs
T21205.hs
T21519a.hs
T21558.hs
T23501a.hs
T23739a.hs
T24810.hs
T24845a.hs
T25180.hs
T25266.hs
T25266b.hs
T26256a.hs
T26345.hs
T26346.hs
T4444.hs
T4524.hs
T5032.hs
T5490.hs
T6018.hs
TcSpecPragmas.hs
UnliftedNewtypesGnd.hs
tc134.hs
tc135.hs
tc192.hs
tc194.hs
tc213.hs
tc218.hs
tc239.hs
tc251.hs
